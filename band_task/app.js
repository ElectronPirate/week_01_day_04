const band = require('./band');
const members = require('./members');


band.addMember(members[0]);
band.addMember(members[1]);
band.addMember(members[2]);
band.addMember(members[3]);

console.log(band);
